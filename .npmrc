# Make `npm version <patch|minor|major>` the one-command release entry point.
# This message is used for both the version commit and the annotated git tag,
# keeping the SSOT flow consistent with the existing release history.
# (tag-version-prefix defaults to "v", matching the vX.Y.Z tags.)
message=chore(release): bump version to %s
